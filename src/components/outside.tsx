import React from "react";

function OutsideIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/park.png" className={className} />
  );
}

export default OutsideIcon;
