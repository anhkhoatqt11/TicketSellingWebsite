import React from "react";

function CourseIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/knowledge.png" className={className} />
  );
}

export default CourseIcon;
