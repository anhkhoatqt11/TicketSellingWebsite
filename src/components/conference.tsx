import React from "react";

function ConferenceIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/meeting.png" className={className} />
  );
}

export default ConferenceIcon;
