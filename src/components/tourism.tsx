import React from "react";

function TourismIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/tour-guide.png" className={className} />
  );
}

export default TourismIcon;
