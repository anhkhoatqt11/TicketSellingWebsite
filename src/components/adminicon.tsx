import React from "react";

function AdminIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Admin" src="/admin.png" className={className} />
  );
}

export default AdminIcon;
