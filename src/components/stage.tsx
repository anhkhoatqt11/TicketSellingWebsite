import React from "react";

function StageIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/watching-a-movie.png" className={className} />
  );
}

export default StageIcon;
