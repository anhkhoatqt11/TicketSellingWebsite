import React from "react";

function Analytics({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Analytics" src="/analytics.png" className={className} />
  );
}

export default Analytics;
