import React from "react";

function SportIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/sport.png" className={className} />
  );
}

export default SportIcon;
