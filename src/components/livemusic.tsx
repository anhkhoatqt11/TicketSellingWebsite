import React from "react";

function LiveMusicIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/guitar.png" className={className} />
  );
}

export default LiveMusicIcon;
