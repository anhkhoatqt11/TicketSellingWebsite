import React from "react";

function CalendarIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/calendar.png" className={className} />
  );
}

export default CalendarIcon;
