import React from "react";

function OrganizerIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Organizer icon" src="/hierarchy.png" className={className} />
  );
}

export default OrganizerIcon;
