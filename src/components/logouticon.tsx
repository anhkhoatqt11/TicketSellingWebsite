import React from "react";

function LogoutIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Logout icon" src="/log-out.png" className={className} />
  );
}

export default LogoutIcon;
