import React from "react";

function TicketIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="User ticket" src="/ticket-icon.png" className={className} />
  );
}

export default TicketIcon;
