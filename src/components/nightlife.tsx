import React from "react";

function NightLifeIcon({ className }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img alt="Live music" src="/tequila-shot.png" className={className} />
  );
}

export default NightLifeIcon;
